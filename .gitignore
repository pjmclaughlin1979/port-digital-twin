node_modules
dist
dev-dist
.DS_Store
*.local

# Separate projects that happen to live alongside this one; each has its
# own git repository and should never be tracked here.
/capacity-map/
/port-ops-twin/

# Personal Claude Code permission overrides, not shared project config.
.claude/settings.local.json
